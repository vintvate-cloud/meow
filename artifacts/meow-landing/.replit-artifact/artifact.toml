kind = "web"
previewPath = "/"
title = "MEOW — Event Platform"
version = "1.0.0"
id = "artifacts/meow-landing"
router = "path"

[[integratedSkills]]
name = "react-vite"
version = "1.0.0"

[[services]]
name = "web"
paths = [ "/" ]
localPort = 20308

[services.development]
run = "pnpm --filter @workspace/meow-landing run dev"

[services.production]
build = [ "pnpm", "--filter", "@workspace/meow-landing", "run", "build" ]
publicDir = "artifacts/meow-landing/dist/public"
serve = "static"

[[services.production.rewrites]]
from = "/*"
to = "/index.html"

[services.env]
PORT = "20308"
BASE_PATH = "/"
